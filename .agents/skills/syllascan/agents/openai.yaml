interface:
  display_name: "Syllascan"
  short_description: "Repo-specific patterns and workflows for syllascan"
  default_prompt: "Use the syllascan repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true